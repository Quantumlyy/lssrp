generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["orderByRelation", "orderByAggregateGroup", "selectRelationCount", "nApi"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  username String @unique

  email EmailService?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model EmailService {
  id Int @id @default(autoincrement())

  username String @unique
  address  String @unique

  user   User @relation(fields: [userId], references: [id])
  userId Int

  sent     Email[] @relation("EmailSender")
  received Email[] @relation("EmailReceiver")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Email {
  id Int @id @default(autoincrement())

  subject  String?
  contents String

  sender   EmailService @relation("EmailSender", fields: [senderId], references: [id])
  senderId Int

  receiver   EmailService @relation("EmailReceiver", fields: [receiverId], references: [id])
  receiverId Int

  createdAt DateTime @default(now())

}
