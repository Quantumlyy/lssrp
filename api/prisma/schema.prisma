generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["orderByRelation", "orderByAggregateGroup", "selectRelationCount", "nApi"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
